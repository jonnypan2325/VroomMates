/**
 * Shared with App.css. The stacked (map-over-panel) layout and the JS that
 * drives the divider must agree on where the layout flips, so the breakpoint
 * lives in one place — update it here and in App.css together.
 */
export const MOBILE_QUERY = '(max-width: 768px)';
